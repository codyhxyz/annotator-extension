# Backend API URL (optional — extension works fully offline without this)
VITE_API_URL=http://localhost:8787

# Clerk publishable key (optional — only needed for cloud sync/auth)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
